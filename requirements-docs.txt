# NOTE: do NOT add `-r requirements.txt` here. This file SHIPS to the public
# repo (publish.LOOSE_FILES) but requirements.txt does not, so the chain breaks
# the public Pages build. For a local preview venv, install BOTH files.
# Build-only dependencies for the MkDocs wiki (build-only; the OKF tooling's own deps are in requirements.txt). mkdocs-material >= 9.5 requires
# MkDocs >= 1.6, needed for validation.links.absolute_links: relative_to_docs.
mkdocs-material>=9.5,<10
# Clean section nav labels (titles from .pages, not directory names).
mkdocs-awesome-pages-plugin>=2.9
# Redirect old URLs after a page/bundle move (e.g. the AI Briefs restructure).
mkdocs-redirects>=1.2
